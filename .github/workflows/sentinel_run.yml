name: WTC Sentinel Execution
on:
  schedule:
    # 13:15 UTC זה 16:15 בישראל
    - cron: '15 13 * * 1-5'  
    
    # השאר נשארים כפי שהם (17:00 ו-23:00)
    - cron: '0 14 * * 1-5'  
    - cron: '0 20 * * 1-5'
  workflow_dispatch: # מאפשר לך להריץ ידנית בלחיצת כפתור לבדיקה

jobs:
  run-sentinel:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write' # קריטי עבור החיבור המאובטח שיצרנו

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install pandas yfinance requests google-api-python-client google-auth-httplib2 google-auth-oauthlib google-genai

      - name: Run Sentinel Script
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python main.py
