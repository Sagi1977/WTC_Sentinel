name: WTC Telegram Bot (DISABLED)
# ─────────────────────────────────────────────────────────────────
# קובץ זה הוחלף על ידי sentinel_run.yml
# sentinel_run.yml מכסה את כל לוח הזמנים הדרוש
# אין צורך להפעיל workflow זה — השאר אותו מבוטל
# ─────────────────────────────────────────────────────────────────

on:
  workflow_dispatch:  # ידני בלבד — אין schedule

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "This workflow is disabled. Use sentinel_run.yml instead."
